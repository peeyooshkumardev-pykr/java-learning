# Array Special Practice — Problem Solving & Logical Thinking

## Q01 — Find the First Local Maximum

Find the first element that is greater than both of its immediate neighbors.

**Example:**
```text
Array = 4 7 3 8 5
Output: 7
```

---

## Q02 — Find an Element Equal to the Average of the Minimum and Maximum

Find an element whose value is exactly the average of the smallest and largest elements in the array.

**Example:**
```text
Array = 2 5 7 10 12
Output: 7
```

---

## Q03 — Find the Closest Element to a Target

Find the array element whose value is closest to the given target.

**Example:**
```text
Array = 4 9 15 21 30
Target = 17
Output: 15
```

---

## Q04 — Find the Second Closest Element to a Target

Find the element with the second-smallest absolute difference from the target.

**Example:**
```text
Array = 2 8 13 19 25
Target = 15
Output: 19
```

---

## Q05 — Find the Element With Maximum Distance From the Average

Calculate the array average and find the element whose absolute distance from the average is greatest.

**Example:**
```text
Array = 2 5 8 20
Average = 8.75
Output: 20
```

---

## Q06 — Find the Smallest Positive Element

Find the smallest element in the array that is greater than zero.

**Example:**
```text
Array = -4 7 2 -1 5
Output: 2
```

---

## Q07 — Find the Largest Negative Element

Find the largest element in the array that is less than zero.

**Example:**
```text
Array = -9 -3 -7 4 2
Output: -3
```

---

## Q08 — Find the Element With the Most Smaller Elements

Find the element that has the greatest number of array elements smaller than it.

**Example:**
```text
Array = 7 2 9 4 6
Output: 9
```

---

## Q09 — Find the First Element Greater Than the Next Two Elements

Find the first element that is greater than both of the next two elements.

**Example:**
```text
Array = 4 9 8 3 7
Output: 9
```

---

## Q10 — Check Whether the Array Forms a Mountain

Determine whether the array strictly increases to one peak and then strictly decreases.

**Example:**
```text
Array = 2 5 8 6 3
Output: Mountain
```

---

## Q11 — Check Whether the Array Forms a Valley

Determine whether the array strictly decreases to one low point and then strictly increases.

**Example:**
```text
Array = 9 6 2 4 7
Output: Valley
```

---

## Q12 — Find the Peak Index of a Mountain

For an array that forms a mountain, find the index of its highest point.

**Example:**
```text
Array = 2 5 9 7 4
Output: 2
```

---

## Q13 — Find the Longest Strictly Alternating Sign Sequence

Find the length of the longest consecutive sequence where positive and negative values strictly alternate.

**Example:**
```text
Array = 2 -3 5 -7 -4 6 -1
Output: 4
```

---

## Q14 — Find the Longest Strictly Alternating Comparison Sequence

Find the length of the longest consecutive sequence where each next value alternately goes up and down.

**Example:**
```text
Array = 1 5 2 6 3 4
Output: 5
```

---

## Q15 — Find the Longest Plateau

Find the length of the longest consecutive sequence of equal elements.

**Example:**
```text
Array = 2 2 5 5 5 3 3
Output: 3
```

---

## Q16 — Find the Longest Strictly Decreasing Run

Find the length of the longest consecutive sequence in which every element is smaller than the previous one.

**Example:**
```text
Array = 9 7 5 6 4 2 1
Output: 4
```

---

## Q17 — Find the First Position Where Left Maximum Equals Right Maximum

Find the first index where the maximum element on the left side equals the maximum element on the right side.

**Example:**
```text
Array = 4 9 2 9 5
Output: 2
```

---

## Q18 — Find the Element Greater Than the Sum of All Other Elements

Find an element whose value is greater than the sum of every other element combined.

**Example:**
```text
Array = 2 3 12 1
Output: 12
```

---

## Q19 — Find the Element Equal to the Sum of All Other Elements

Find an element whose value equals the sum of all the other elements combined.

**Example:**
```text
Array = 2 3 5 10
Output: 10
```

---

## Q20 — Find the Element Closest to the Median

Find the array element whose value is closest to the median of the array values.

**Example:**
```text
Array = 9 2 7 4 12
Median = 7
Output: 7
```

---

## Q21 — Find the Smallest Range Containing at Least One Even and One Odd

Find the smallest difference between an even element and an odd element.

**Example:**
```text
Array = 8 3 10 5 14
Output: 2
```

---

## Q22 — Find the Smallest Range Containing a Positive and a Negative

Find the smallest absolute difference between one positive and one negative element.

**Example:**
```text
Array = -8 -3 2 7
Output: 5
```

---

## Q23 — Find the Closest Pair of Opposite Signs

Find one positive and one negative element whose absolute values are closest.

**Example:**
```text
Array = -10 -4 3 8
Output: -4 3
```

---

## Q24 — Find the Pair With the Largest Sum of Absolute Values

Find two different elements whose absolute values have the largest possible sum.

**Example:**
```text
Array = -9 4 -6 7 2
Output: -9 7
```

---

## Q25 — Find Three Elements With the Smallest Range

Find three different elements whose maximum value minus minimum value is as small as possible.

**Example:**
```text
Array = 10 4 7 15 6
Output: 4 6 7
```

---

## Q26 — Find Three Elements With the Largest Range

Find three different elements whose maximum value minus minimum value is as large as possible.

**Example:**
```text
Array = 3 11 5 2 9
Output: 2 3 11
```

---

## Q27 — Find Three Elements Whose Values Are All Different Modulo K

Find three different elements whose remainders after division by K are all different.

**Example:**
```text
Array = 4 7 10 13 16
K = 3
Output: 4 7 13
```

---

## Q28 — Find the Longest Subsequence With Alternating Parity

Find the maximum number of elements that can be selected in their original order so that even and odd values alternate.

**Example:**
```text
Array = 2 4 7 9 6 3
Output: 4
```

---

## Q29 — Find the Maximum Length Subsequence With Strictly Increasing Values

Find the maximum number of elements that can be selected in original order with each selected value greater than the previous one.

**Example:**
```text
Array = 3 1 4 2 5 3 6
Output: 4
```

---

## Q30 — Find the Minimum Number of Elements Needed to Make a Given Sum Exactly

Find the minimum number of array elements whose values add exactly to the target.

**Example:**
```text
Array = 2 3 7 8 10
Target = 10
Output: 2
```

---

## Q31 — Find Whether Any Subset Has the Given Sum

Determine whether some selection of array elements can add exactly to the target.

**Example:**
```text
Array = 3 4 7 9
Target = 11
Output: Yes
```

---

## Q32 — Find the Maximum Subset Sum With an Even Total

Choose any elements so that their total sum is even and as large as possible.

**Example:**
```text
Array = 3 8 5 6
Output: 14
```

---

## Q33 — Find the Minimum Subset Sum With an Odd Total

Choose any elements so that their total sum is odd and as small as possible, using at least one element.

**Example:**
```text
Array = 4 6 9 12
Output: 9
```

---

## Q34 — Find the Maximum Number of Elements With Pairwise Different Values

Select as many elements as possible while no two selected elements have the same value.

**Example:**
```text
Array = 2 3 2 5 3 7
Output: 4
```

---

## Q35 — Find the Minimum Removals to Make Even and Odd Counts Equal

Find the minimum number of elements that must be ignored so that the remaining elements contain equal numbers of even and odd values.

**Example:**
```text
Array = 2 5 4 7 8
Output: 1
```

---

## Q36 — Find the Minimum Removals to Make Positive and Negative Counts Equal

Find the minimum number of elements that must be ignored so that the remaining elements contain equal numbers of positive and negative values.

**Example:**
```text
Array = 2 -1 4 -3 -5 7 8
Output: 1
```

---

## Q37 — Find the Maximum Sum of Two Non-Equal Elements

Find two different elements with different values whose sum is maximum.

**Example:**
```text
Array = 8 8 5 7 3
Output: 15
```

---

## Q38 — Find the Minimum Product of Two Positive Elements

Find two different positive elements whose product is minimum.

**Example:**
```text
Array = -3 7 2 9 4
Output: 14
```

---

## Q39 — Find the Maximum Product of Two Negative Elements

Find two different negative elements whose product is maximum.

**Example:**
```text
Array = -8 -3 -5 4
Output: 40
```

---

## Q40 — Find the Pair With the Smallest Sum of Absolute Values

Find two different elements for which the sum of their absolute values is minimum.

**Example:**
```text
Array = -8 3 -2 6 5
Output: -2 3
```

---

## Q41 — Find the First Pair Whose Values Have Equal Number of Digits

Find the first pair of different elements that contain the same number of decimal digits.

**Example:**
```text
Array = 7 42 5 103
Output: 7 5
```

---

## Q42 — Find the Longest Run With the Same Parity

Find the longest consecutive sequence in which all elements have the same parity.

**Example:**
```text
Array = 2 4 8 5 7 6 10
Output: 3
```

---

## Q43 — Find the Longest Prefix With More Even Than Odd Elements

Find the length of the longest prefix in which the number of even elements is greater than the number of odd elements.

**Example:**
```text
Array = 2 4 7 9 6
Output: 3
```

---

## Q44 — Find the Longest Suffix With More Positive Than Negative Elements

Find the length of the longest suffix in which positive elements are more numerous than negative elements.

**Example:**
```text
Array = -2 4 -1 6 8
Output: 4
```

---

## Q45 — Find the Maximum Sum of Elements at Prime Indices

Using zero-based indexing, find the sum of elements whose indices are prime numbers.

**Example:**
```text
Array = 5 8 3 7 2 9 4
Output: 19
```

---

## Q46 — Find the Element Whose Index Equals Its Value

Find an element whose value is equal to its zero-based index.

**Example:**
```text
Array = 5 2 1 3 4
Output: 1
```

---

## Q47 — Find the Sum of Elements at Odd Indices

Using zero-based indexing, find the sum of all elements stored at odd indices.

**Example:**
```text
Array = 4 9 7 2 8 5
Output: 16
```

---

## Q48 — Find the Product of Elements at Even Indices

Using zero-based indexing, find the product of all elements stored at even indices.

**Example:**
```text
Array = 2 6 3 5 4
Output: 24
```

---

## Q49 — Find the Longest Subsequence With Values Divisible by Their Position

Using one-based positions, find the maximum number of elements that can be selected in original order where each selected value is divisible by its selected position.

**Example:**
```text
Array = 2 4 6 8 10
Output: 5
```

---

## Q50 — Find the Maximum Number of Disjoint Pairs With Product Divisible by K

Form disjoint pairs, using each array element at most once, so that as many pairs as possible have a product divisible by K.

**Example:**
```text
Array = 2 3 4 5 6 9
K = 6
Output: 3
```

---

