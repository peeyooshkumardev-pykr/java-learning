# 🔵 Java Arrays — Level 4: Array Manipulation & Advanced Logic

### Q01 — Reverse an Array Without Using Another Array

Take an array from the user and reverse the array in the same array without creating another array.

**Example:**

```text
Enter N = 5
Enter elements:
10 20 30 40 50

Array after reverse:
50 40 30 20 10
```

---

### Q02 — Rotate an Array One Position to the Left

Take an array and rotate all elements one position to the left.

**Example:**

```text
Enter N = 5
Enter elements:
10 20 30 40 50

After left rotation:
20 30 40 50 10
```

---

### Q03 — Rotate an Array One Position to the Right

Take an array and rotate all elements one position to the right.

**Example:**

```text
Enter N = 5
Enter elements:
10 20 30 40 50

After right rotation:
50 10 20 30 40
```

---

### Q04 — Rotate an Array Left by K Positions

Take an array and an integer `K`. Rotate the array to the left by `K` positions.

**Example:**

```text
Enter N = 6
Enter elements:
10 20 30 40 50 60

Enter K = 2

After rotation:
30 40 50 60 10 20
```

---

### Q05 — Rotate an Array Right by K Positions

Take an array and an integer `K`. Rotate the array to the right by `K` positions.

**Example:**

```text
Enter N = 6
Enter elements:
10 20 30 40 50 60

Enter K = 2

After rotation:
50 60 10 20 30 40
```

---

### Q06 — Move All Zeros to the End

Take an array and move all zero elements to the end while maintaining the order of non-zero elements.

**Example:**

```text
Enter N = 7
Enter elements:
0 10 0 20 30 0 40

After rearranging:
10 20 30 40 0 0 0
```

---

### Q07 — Move All Negative Elements to the Beginning

Take an array and move all negative elements to the beginning of the array.

**Example:**

```text
Enter N = 7
Enter elements:
10 -5 20 -8 30 -2 40

After rearranging:
-5 -8 -2 10 20 30 40
```

---

### Q08 — Separate Even and Odd Elements

Take an array and rearrange it so that all even elements appear before all odd elements.

**Example:**

```text
Enter N = 7
Enter elements:
11 20 15 8 30 7 12

After rearranging:
20 8 30 12 11 15 7
```

---

### Q09 — Separate Positive and Negative Elements

Take an array and rearrange it so that positive elements appear before negative elements.

**Example:**

```text
Enter N = 7
Enter elements:
-10 20 -5 30 -8 15 40

After rearranging:
20 30 15 40 -10 -5 -8
```

---

### Q10 — Merge Two Arrays

Take two arrays from the user and create a third array containing all elements of both arrays.

**Example:**

```text
Array 1:
10 20 30

Array 2:
40 50 60

Merged array:
10 20 30 40 50 60
```

---

### Q11 — Merge Two Sorted Arrays

Take two sorted arrays and merge them into a single sorted array.

**Example:**

```text
Array 1:
10 30 50 70

Array 2:
20 40 60 80

Merged array:
10 20 30 40 50 60 70 80
```

---

### Q12 — Insert an Element at a Given Position

Take an array, an element, and an index. Insert the element at the given position.

**Example:**

```text
Array:
10 20 30 40 50

Enter element = 25
Enter index = 2

After insertion:
10 20 25 30 40 50
```

---

### Q13 — Delete an Element from a Given Position

Take an array and an index. Delete the element present at that index.

**Example:**

```text
Array:
10 20 30 40 50

Enter index = 2

After deletion:
10 20 40 50
```

---

### Q14 — Delete All Occurrences of a Given Element

Take an array and an element. Remove every occurrence of that element.

**Example:**

```text
Array:
10 20 10 30 10 40

Enter element = 10

After deletion:
20 30 40
```

---

### Q15 — Shift All Elements One Position to the Left

Shift every element one position to the left. The first element should be removed.

**Example:**

```text
Array:
10 20 30 40 50

After left shift:
20 30 40 50
```

---

### Q16 — Shift All Elements One Position to the Right

Shift every element one position to the right. The last position becomes available for a new value.

**Example:**

```text
Array:
10 20 30 40 50

Enter new element = 5

After right shift:
5 10 20 30 40
```

---

### Q17 — Rearrange Array in Maximum-Minimum Form

Given a sorted array, rearrange it so that the maximum element comes first, then the minimum, then the second maximum, then the second minimum, and so on.

**Example:**

```text
Array:
1 2 3 4 5 6

After rearranging:
6 1 5 2 4 3
```

---

### Q18 — Rearrange Positive and Negative Elements Alternately

Rearrange an array so that positive and negative elements appear alternately whenever possible.

**Example:**

```text
Array:
1 2 -3 -4 5 -6

After rearranging:
1 -3 2 -4 5 -6
```

---

### Q19 — Sort an Array in Ascending Order Without Arrays.sort()

Take an array and sort its elements in ascending order without using `Arrays.sort()`.

**Example:**

```text
Array:
40 10 50 20 30

Sorted array:
10 20 30 40 50
```

---

### Q20 — Sort an Array in Descending Order Without Arrays.sort()

Take an array and sort its elements in descending order without using `Arrays.sort()`.

**Example:**

```text
Array:
40 10 50 20 30

Sorted array:
50 40 30 20 10
```
