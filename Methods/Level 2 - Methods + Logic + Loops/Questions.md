# Java Methods — Level 2: Methods + Logic + Loops

### Q01 — Method Returning Positive, Negative, or Zero

Create a method named `checkNumber()` that accepts an integer and returns a `String` indicating whether the number is positive, negative, or zero.

Example:

```text
Enter number = -8

Number is Negative
```

---

### Q02 — Method to Check Divisibility by 3 and 5

Create a method named `isDivisibleByBoth()` that accepts an integer and returns `true` if the number is divisible by both 3 and 5, otherwise returns `false`.

Example:

```text
Enter number = 30

Divisible by both = true
```

---

### Q03 — Method to Find the Smallest of Three Numbers

Create a method named `findSmallest()` that accepts three integers and returns the smallest number.

Example:

```text
Enter first number = 25
Enter second number = 48
Enter third number = 12

Smallest = 12
```

---

### Q04 — Method to Check Leap Year

Create a method named `isLeapYear()` that accepts a year and returns `true` if the year is a leap year, otherwise returns `false`.

Example:

```text
Enter year = 2024

Leap Year = true
```

---

### Q05 — Method to Check Divisibility

Create a method named `isDivisible()` that accepts two integers and returns whether the first number is divisible by the second number.

Example:

```text
Enter number = 30
Enter divisor = 5

Divisible = true
```

---

### Q06 — Method to Calculate Factorial

Create a method named `factorial()` that accepts an integer and returns its factorial.

Example:

```text
Enter N = 5

Factorial = 120
```

---

### Q07 — Method to Calculate Sum from 1 to N

Create a method named `calculateSum()` that accepts an integer `N` and returns the sum of numbers from `1` to `N`.

Example:

```text
Enter N = 10

Sum = 55
```

---

### Q08 — Method to Print Multiplication Table

Create a method named `printTable()` that accepts an integer and prints its multiplication table from 1 to 10.

Example:

```text
Enter number = 7

7 x 1 = 7
7 x 2 = 14
7 x 3 = 21
7 x 4 = 28
7 x 5 = 35
7 x 6 = 42
7 x 7 = 49
7 x 8 = 56
7 x 9 = 63
7 x 10 = 70
```

---

### Q09 — Method to Count Digits

Create a method named `countDigits()` that accepts an integer and returns the number of digits in it.

Example:

```text
Enter number = 58392

Number of digits = 5
```

---

### Q10 — Method to Find Sum of Digits

Create a method named `sumOfDigits()` that accepts an integer and returns the sum of its digits.

Example:

```text
Enter number = 5832

Sum of digits = 18
```

---

### Q11 — Method to Reverse a Number

Create a method named `reverseNumber()` that accepts an integer and returns its reverse.

Example:

```text
Enter number = 12345

Reverse = 54321
```

---

### Q12 — Method to Check Number Palindrome

Create a method named `isPalindrome()` that accepts an integer and returns `true` if the number is a palindrome, otherwise returns `false`.

Example:

```text
Enter number = 1221

Palindrome = true
```

---

### Q13 — Method to Check Prime Number

Create a method named `isPrime()` that accepts an integer and returns `true` if the number is prime and `false` otherwise.

Example:

```text
Enter number = 29

Prime = true
```

---

### Q14 — Method to Print Prime Numbers in a Range

Create a method named `printPrimes()` that accepts two integers representing a range and prints all prime numbers within that range.

Example:

```text
Enter start = 10
Enter end = 30

Prime numbers:
11 13 17 19 23 29
```

---

### Q15 — Method to Find GCD

Create a method named `findGCD()` that accepts two integers and returns their Greatest Common Divisor.

Example:

```text
Enter first number = 24
Enter second number = 36

GCD = 12
```

---

### Q16 — Method to Find LCM

Create a method named `findLCM()` that accepts two integers and returns their Least Common Multiple.

Example:

```text
Enter first number = 12
Enter second number = 18

LCM = 36
```

---

### Q17 — Method to Check Armstrong Number

Create a method named `isArmstrong()` that accepts an integer and returns `true` if the number is an Armstrong number, otherwise returns `false`.

Example:

```text
Enter number = 153

Armstrong = true
```

---

### Q18 — Method to Check Perfect Number

Create a method named `isPerfect()` that accepts an integer and returns `true` if the number is a perfect number, otherwise returns `false`.

Example:

```text
Enter number = 28

Perfect = true
```

---

### Q19 — Method to Count Even Numbers in a Range

Create a method named `countEvenNumbers()` that accepts a range from `start` to `end` and returns the number of even numbers present in that range.

Example:

```text
Enter start = 1
Enter end = 10

Even numbers = 5
```

---

### Q20 — Build a Number Analyzer Using Multiple Methods

Create a program that accepts an integer and uses separate methods to:

* Count its digits
* Find the sum of its digits
* Reverse the number
* Check whether it is a palindrome
* Check whether it is prime
* Check whether it is an Armstrong number

Example:

```text
Enter number = 153

Number of digits = 3
Sum of digits = 9
Reverse = 351
Palindrome = false
Prime = false
Armstrong = true
```

---

# Level 2 Complete — 20 Questions

By the end of this level, you should be comfortable using methods with:

```text
if-else
   ↓
boolean logic
   ↓
for / while loops
   ↓
number calculations
   ↓
digit manipulation
   ↓
methods calling methods
   ↓
range-based logic
   ↓
multiple methods working together
```


