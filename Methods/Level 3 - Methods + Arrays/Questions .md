# Java Methods — Level 3: Methods + Arrays

### Q01 — Method to Print an Array

Create a method named `printArray()` that accepts an integer array and prints all its elements.

Example:

```text
Enter array size = 5
Enter elements:
10 20 30 40 50

Array:
10 20 30 40 50
```

---

### Q02 — Method to Calculate Array Sum

Create a method named `calculateSum()` that accepts an integer array and returns the sum of all elements.

Example:

```text
Enter array size = 5
Enter elements:
10 20 30 40 50

Sum = 150
```

---

### Q03 — Method to Calculate Array Average

Create a method named `calculateAverage()` that accepts an integer array and returns its average.

Example:

```text
Enter array size = 4
Enter elements:
10 20 30 40

Average = 25.0
```

---

### Q04 — Method to Find Largest Array Element

Create a method named `findLargest()` that accepts an integer array and returns the largest element.

Example:

```text
Enter array size = 5
Enter elements:
12 45 7 89 23

Largest = 89
```

---

### Q05 — Method to Find Smallest Array Element

Create a method named `findSmallest()` that accepts an integer array and returns the smallest element.

Example:

```text
Enter array size = 5
Enter elements:
12 45 7 89 23

Smallest = 7
```

---

### Q06 — Method to Count Even and Odd Elements

Create a method named `countEvenOdd()` that accepts an integer array and counts how many elements are even and how many are odd.

Example:

```text
Enter array size = 6
Enter elements:
10 15 22 31 40 51

Even = 3
Odd = 3
```

---

### Q07 — Method to Count Positive, Negative, and Zero

Create a method named `countNumbers()` that accepts an integer array and counts positive numbers, negative numbers, and zeros.

Example:

```text
Enter array size = 7
Enter elements:
10 -5 0 20 -8 0 15

Positive = 3
Negative = 2
Zero = 2
```

---

### Q08 — Method to Search an Element

Create a method named `searchElement()` that accepts an integer array and a target value. Return `true` if the target exists in the array and `false` otherwise.

Example:

```text
Enter array size = 5
Enter elements:
10 20 30 40 50

Enter target = 30

Found = true
```

---

### Q09 — Method to Find Element Index

Create a method named `findIndex()` that accepts an integer array and a target value. Return the index of the first occurrence of the target.

If the element does not exist, return `-1`.

Example:

```text
Enter array size = 5
Enter elements:
10 20 30 40 50

Enter target = 40

Index = 3
```

---

### Q10 — Method to Count Occurrences

Create a method named `countOccurrences()` that accepts an integer array and a target value. Return how many times the target appears.

Example:

```text
Enter array size = 7
Enter elements:
10 20 10 30 10 40 10

Enter target = 10

Occurrences = 4
```

---

### Q11 — Method to Reverse an Array

Create a method named `reverseArray()` that accepts an integer array and prints its elements in reverse order.

Example:

```text
Enter array size = 5
Enter elements:
10 20 30 40 50

Reversed array:
50 40 30 20 10
```

---

### Q12 — Method to Copy an Array

Create a method named `copyArray()` that accepts an integer array and returns a copy of the array.

Example:

```text
Enter array size = 4
Enter elements:
5 10 15 20

Copied array:
5 10 15 20
```

---

### Q13 — Method to Check Whether an Array Is Sorted

Create a method named `isSorted()` that accepts an integer array and returns `true` if the array is sorted in ascending order and `false` otherwise.

Example:

```text
Enter array size = 5
Enter elements:
10 20 30 40 50

Sorted = true
```

---

### Q14 — Method to Find Second Largest Element

Create a method named `findSecondLargest()` that accepts an integer array and returns the second-largest distinct element.

Example:

```text
Enter array size = 6
Enter elements:
10 45 20 89 45 30

Second largest = 45
```

---

### Q15 — Method to Find Second Smallest Element

Create a method named `findSecondSmallest()` that accepts an integer array and returns the second-smallest distinct element.

Example:

```text
Enter array size = 6
Enter elements:
40 10 25 5 30 5

Second smallest = 10
```

---

### Q16 — Method to Compare Two Arrays

Create a method named `areEqual()` that accepts two integer arrays and returns `true` if both arrays contain the same elements in the same order.

Example:

```text
First array:
10 20 30 40

Second array:
10 20 30 40

Arrays equal = true
```

---

### Q17 — Method to Find Common Elements

Create a method named `printCommonElements()` that accepts two integer arrays and prints the elements that appear in both arrays.

Example:

```text
First array:
10 20 30 40 50

Second array:
20 40 60 80 10

Common elements:
10 20 40
```

---

### Q18 — Method to Remove Duplicate Elements

Create a method named `printUniqueElements()` that accepts an integer array and prints each distinct element only once.

Example:

```text
Enter array:
10 20 10 30 20 40 10

Unique elements:
10 20 30 40
```

---

### Q19 — Create Multiple Array Methods

Create separate methods to:

* Calculate the sum
* Find the largest element
* Find the smallest element
* Count even elements
* Count odd elements

Call all methods from `main()` for the same array.

Example:

```text
Enter array:
10 25 30 45 50

Sum = 160
Largest = 50
Smallest = 10
Even = 3
Odd = 2
```

---

### Q20 — Build an Array Analyzer Using Multiple Methods

Create a program that accepts an integer array and uses separate methods to:

* Print the array
* Find the sum
* Calculate the average
* Find the largest element
* Find the smallest element
* Find the second-largest element
* Count even and odd elements
* Search for a target element
* Check whether the array is sorted
* Print the reversed array

Example:

```text
Enter array size = 6
Enter elements:
10 25 30 45 50 60

Array:
10 25 30 45 50 60

Sum = 220
Average = 36.666666666666664
Largest = 60
Smallest = 10
Second largest = 50
Even = 4
Odd = 2

Enter target = 45
Found = true

Sorted = true

Reversed array:
60 50 45 30 25 10
```

---

## Level 3 Complete — 20 Questions

After Level 3, you should be comfortable with:

```text
Array
  ↓
Pass array to method
  ↓
Process array inside method
  ↓
Return result
  ↓
Multiple methods
  ↓
Methods working with the same array
  ↓
Methods + loops + conditions
```

